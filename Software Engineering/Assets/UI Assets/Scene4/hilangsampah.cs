using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class hilangsampah : MonoBehaviour
{
    void Update()
    {
        //Invoke("ded", 20);
    }

    private void ded()
    {
        Destroy(gameObject);
    }
}
